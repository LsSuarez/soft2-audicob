@model IEnumerable<Audicob.Models.HistorialCredito>

<table class="table table-striped table-bordered text-center align-middle">
    <thead class="table-dark">
        <tr>
            <th>Nombre del Cliente</th>
            <th>Fecha de Operación</th>
            <th>Estado de Pago</th>
            <th>Acción</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.NombreCliente</td>
                    <td>@item.FechaOperacion.ToString("dd/MM/yyyy")</td>
                    <td>@item.EstadoPago</td>
                    <td>
                        <button class="btn btn-info btn-sm btn-detalle" data-id="@item.Id">
                            🔍 Detalle
                        </button>
                        <a class="btn btn-danger btn-sm"
                           href="/Supervisor/ExportarHistorialCreditoPDF/@item.Id"
                           target="_blank">
                            📄 Exportar
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="4" class="text-muted">No se encontraron registros</td>
            </tr>
        }
    </tbody>
</table>
